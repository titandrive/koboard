local _ = require("gettext")
return {
    name = "koboard",
    fullname = _("KOBoard"),
    version = "0.6.0",
    description = _([[Forces Android system keyboard for all text input, replacing KOReader's built-in keyboard.]]),
}
